# Copia in `.env.local` e riempi con il tuo URL Worker/VPS
# Esempio Contabo: VITE_WORKER_URL=http://81.17.100.112:3000
VITE_WORKER_URL=

# Bearer token per autenticare le chiamate POST (start/stop/reset)
# Deve coincidere col API_TOKEN del server (.env)
VITE_WORKER_AUTH=
